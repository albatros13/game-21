/**
 * Game commands available to players
 * @type {{HIT: string, STAND: string, SPLIT: string}}
 */
export const Command = {
    INITIAL: 'Initial', //In case we need to remember how each card was obtained
    HIT    : 'Hit',
    STAND  : 'Stand',
    SPLIT  : 'Split'
};

